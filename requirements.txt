# Core dependencies
requests>=2.31.0
yfinance>=1.6.0
pandas>=2.0.0
ddgs>=0.2.0
python-dotenv>=1.0.0
pydantic>=2.0.0

# Technical analysis (requires Python 3.12+ for numba)
# pandas-ta>=0.4.71b0  # Requires Python 3.12+ for numba
scipy>=1.10.0
numpy>=1.24.0

# News & data enrichment
feedparser>=6.0.10
finvizfinance>=1.0.0

# Optional: TA-Lib for 60+ candlestick patterns (requires Visual C++ Build Tools on Windows)
# ta-lib>=0.4.24  # pip install ta-lib (needs: brew install ta-lib / apt-get install libta-lib-dev)

# Optional: FinBERT for better sentiment (uncomment if needed)
# transformers>=4.30.0
# torch>=2.0.0